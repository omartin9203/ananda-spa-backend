import { IFieldFilterOptions } from './IFieldFilterOptions';

export class FilterStringOptions extends IFieldFilterOptions<string> {
  // $contains?: { value: string; options?: string };
  // $eq?: string;
  // $in?: string[];
  // $ne?: string;
  // $regex?: RegExp;
}
